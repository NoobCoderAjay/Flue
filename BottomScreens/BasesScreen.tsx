import { StyleSheet, Text, View } from "react-native";
import React from "react";

interface Props {}

const BasesScreen = (props: Props) => {
  return (
    <View>
      <Text>BasesScreen</Text>
    </View>
  );
};

export default BasesScreen;

const styles = StyleSheet.create({});
